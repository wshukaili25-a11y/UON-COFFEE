
import {createClient} from 'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type','Access-Control-Allow-Methods':'POST,OPTIONS'};const response=(b:any,s=200)=>new Response(typeof b==='string'?b:JSON.stringify(b),{status:s,headers:{...cors,'content-type':'application/json'}});
const db=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);
const TOKEN=Deno.env.get('WHATSAPP_ACCESS_TOKEN');const PHONE_ID=Deno.env.get('WHATSAPP_PHONE_NUMBER_ID');const TEMPLATE=Deno.env.get('WHATSAPP_TEMPLATE_NAME')||'uon_hub_notification';const LANG=Deno.env.get('WHATSAPP_TEMPLATE_LANGUAGE')||'ar';
Deno.serve(async req=>{if(req.method==='OPTIONS')return response('',204);try{if(!TOKEN||!PHONE_ID)throw new Error('WhatsApp secrets missing');const p=await req.json();const to=String(p.to||Deno.env.get('WHATSAPP_ADMIN_PHONE')||'').replace(/\D/g,'');if(!to)throw new Error('recipient missing');
 const payload={messaging_product:'whatsapp',to,type:'template',template:{name:TEMPLATE,language:{code:LANG},components:[{type:'body',parameters:[{type:'text',text:p.message||'اختبار إشعارات UON Hub'}]}]}};
 const r=await fetch(`https://graph.facebook.com/v23.0/${PHONE_ID}/messages`,{method:'POST',headers:{Authorization:`Bearer ${TOKEN}`,'content-type':'application/json'},body:JSON.stringify(payload)});const j=await r.json();await db.from('notification_log').insert({channel:'whatsapp',event_type:p.type||'test',destination:to,status:r.ok?'sent':'failed',error:r.ok?null:JSON.stringify(j),payload});if(!r.ok)throw new Error(JSON.stringify(j));return response({ok:true,result:j})}catch(e){return response({ok:false,error:String(e.message||e)},500)}});
