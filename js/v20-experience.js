import {get,insert,esc,toast,trackEvent,rpc} from './core.js?v=30.0.1';

if(!document.querySelector('link[data-v30-mobile]')){
 const mobileCss=document.createElement('link');
 mobileCss.rel='stylesheet';
 mobileCss.href='/css/v30-mobile.css?v=30.0.1';
 mobileCss.dataset.v30Mobile='1';
 document.head.append(mobileCss);
}

const PAGE_KEY='uon_favorites_v20';
const contributionKey='uon_contributions_v20';
const current={title:document.title.replace(/\s*\|.*$/,''),url:location.pathname.split('/').pop()||'index.html'};

function readJson(key,fallback){try{return JSON.parse(localStorage.getItem(key)||JSON.stringify(fallback))}catch{return fallback}}
function writeJson(key,value){localStorage.setItem(key,JSON.stringify(value))}
function favorites(){return readJson(PAGE_KEY,[])}
function isFavorite(){return favorites().some(x=>x.url===current.url)}
function toggleFavorite(){const rows=favorites();const i=rows.findIndex(x=>x.url===current.url);if(i>=0){rows.splice(i,1);toast('تمت إزالة الصفحة من المفضلة')}else{rows.unshift({...current,added_at:new Date().toISOString()});toast('تم حفظ الصفحة في المفضلة ❤️');trackEvent('favorite_add',current)}writeJson(PAGE_KEY,rows.slice(0,80));syncButtons()}
function syncButtons(){document.querySelectorAll('[data-v20-favorite]').forEach(b=>{b.textContent=isFavorite()?'♥':'♡';b.title=isFavorite()?'إزالة من المفضلة':'حفظ في المفضلة'})}
function openQr(){const old=document.querySelector('#v20QrModal');if(old)old.remove();const url=location.href;const m=document.createElement('div');m.id='v20QrModal';m.className='v20-modal open';m.innerHTML=`<div class="v20-modal-card"><button class="v20-close" aria-label="إغلاق">×</button><h3>شارك الصفحة</h3><img alt="QR Code" src="https://api.qrserver.com/v1/create-qr-code/?size=240x240&data=${encodeURIComponent(url)}"><p>${esc(document.title)}</p><button class="btn primary" data-copy>نسخ الرابط</button></div>`;document.body.append(m);m.querySelector('.v20-close').onclick=()=>m.remove();m.onclick=e=>{if(e.target===m)m.remove()};m.querySelector('[data-copy]').onclick=async()=>{await navigator.clipboard.writeText(url);toast('تم نسخ الرابط')}}
function openFeedback(){const old=document.querySelector('#v20FeedbackModal');if(old)old.remove();const m=document.createElement('div');m.id='v20FeedbackModal';m.className='v20-modal open';m.innerHTML=`<form class="v20-modal-card"><button type="button" class="v20-close">×</button><h3>كيف كانت تجربتك؟</h3><div class="v20-stars">${[1,2,3,4,5].map(n=>`<button type="button" data-rate="${n}">☆</button>`).join('')}</div><textarea name="comment" placeholder="ملاحظتك اختيارية"></textarea><button class="btn primary" type="submit">إرسال التقييم</button></form>`;document.body.append(m);let rating=0;const stars=[...m.querySelectorAll('[data-rate]')];stars.forEach(b=>b.onclick=()=>{rating=Number(b.dataset.rate);stars.forEach((x,i)=>x.textContent=i<rating?'★':'☆')});m.querySelector('.v20-close').onclick=()=>m.remove();m.querySelector('form').onsubmit=async e=>{e.preventDefault();if(!rating){toast('اختر عدد النجوم',true);return}const btn=e.submitter;btn.disabled=true;try{await insert('platform_feedback',{rating,comment:new FormData(e.target).get('comment')||null,page_path:location.pathname,status:'pending'},{returning:false});toast('شكرًا لتقييمك 💙');m.remove()}catch(err){toast(err.message,true)}finally{btn.disabled=false}}}
function addUtilityDock(){if(document.body.classList.contains('admin-page')||location.pathname.endsWith('/admin.html')||document.querySelector('.v20-utility-dock'))return;const dock=document.createElement('div');dock.className='v20-utility-dock';dock.innerHTML=`<button data-v20-favorite title="المفضلة">♡</button><button data-v20-qr title="QR">▦</button><button data-v20-feedback title="قيّم المنصة">★</button>`;document.body.append(dock);dock.querySelector('[data-v20-favorite]').onclick=toggleFavorite;dock.querySelector('[data-v20-qr]').onclick=openQr;dock.querySelector('[data-v20-feedback]').onclick=openFeedback;syncButtons()}
async function updateBadge(){try{const rows=await get('site_updates','select=id&active=eq.true&order=created_at.desc&limit=20');const seen=Number(localStorage.getItem('uon_updates_seen')||0);if(rows.length>seen){const a=document.createElement('a');a.href='updates.html';a.className='v20-update-badge';a.textContent=`${rows.length-seen} تحديث جديد`;document.body.append(a)}}catch{}}
function normalizeFooterUrl(value=''){const raw=String(value||'').trim();if(!raw)return'';if(/^https?:\/\//i.test(raw))return raw;if(/^www\./i.test(raw))return`https://${raw}`;if(/^instagram\.com\//i.test(raw)||/^x\.com\//i.test(raw)||/^twitter\.com\//i.test(raw)||/^tiktok\.com\//i.test(raw)||/^linkedin\.com\//i.test(raw))return`https://${raw}`;return''}
async function applyManagedFooter(){const footer=document.querySelector('.site-footer');if(!footer)return;const defaults={footer_top_text:'رَبِّ زِدْنِي عِلْمًا',footer_credit_prefix:'صُمم بحب من طلاب جامعة نزوى ❤️',footer_credit_label:'@uonhub',footer_credit_url:'',footer_rights:'جميع الحقوق محفوظة © 2026 UON Hub'};let m={...defaults};try{const keys=Object.keys(defaults).join(',');const rows=await get('site_settings',`select=key,value&key=in.(${keys})`);for(const row of rows||[])if(row?.key&&row.value!==null&&row.value!==undefined)m[row.key]=String(row.value)}catch{}let accounts=[];try{accounts=await get('footer_social_links','select=id,label,url,sort_order,active&active=eq.true&order=sort_order.asc,created_at.asc')}catch{}if(!accounts.length&&m.footer_credit_label)accounts=[{label:m.footer_credit_label,url:m.footer_credit_url||'',active:true}];const accountHtml=accounts.map(item=>{const label=esc(item.label||'');const url=normalizeFooterUrl(item.url||'');return url?`<a class="footer-social-link" href="${esc(url)}" target="_blank" rel="noopener noreferrer">${label}</a>`:`<span class="footer-social-link is-disabled">${label}</span>`}).join('');const container=footer.querySelector('.container')||footer;container.classList.remove('footer-row');container.classList.add('footer-managed');container.innerHTML=`<p class="footer-top-text">${esc(m.footer_top_text)}</p><p class="footer-credit"><span>${esc(m.footer_credit_prefix)}</span></p>${accountHtml?`<div class="footer-social-links" aria-label="حسابات المنصة">${accountHtml}</div>`:''}<p class="footer-rights-text">${esc(m.footer_rights)}</p>`}
function safeNotificationUrl(value=''){const raw=String(value||'').trim();if(!raw)return'';if(/^https?:\/\//i.test(raw))return raw;if(/^[a-z0-9_-]+\.html(?:[?#].*)?$/i.test(raw))return raw;return''}
async function setupNotifications(){const drawer=document.querySelector('#notificationDrawer');const items=document.querySelector('#notificationItems');if(!drawer||!items)return;let opener=document.querySelector('[data-notifications-open],#openNotifications,#notificationButton');if(!opener){opener=document.createElement('button');opener.type='button';opener.className='icon-btn uon-notification-button';opener.dataset.notificationsOpen='1';opener.setAttribute('aria-label','الإشعارات');opener.innerHTML='🔔<span class="uon-notification-badge" hidden></span>';const actions=document.querySelector('.nav-actions,.v176-nav-actions');actions?.prepend(opener)}const close=document.querySelector('#closeNotifications');const setOpen=value=>{drawer.classList.toggle('open',value);drawer.toggleAttribute('inert',!value);drawer.setAttribute('aria-hidden',String(!value));opener?.setAttribute('aria-expanded',String(value));document.body.classList.toggle('notification-open',value);if(value)close?.focus();else opener?.focus()};drawer.setAttribute('role','dialog');drawer.setAttribute('aria-modal','true');drawer.setAttribute('aria-hidden','true');drawer.setAttribute('inert','');opener?.setAttribute('aria-expanded','false');opener?.setAttribute('aria-controls',drawer.id);opener?.addEventListener('click',()=>setOpen(true));close?.addEventListener('click',()=>setOpen(false));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&drawer.classList.contains('open'))setOpen(false)});try{const rows=await get('site_notifications','select=id,title,body,icon,url,created_at&active=eq.true&order=created_at.desc&limit=50');const lastSeen=localStorage.getItem('uon_notifications_seen_at')||'';const unread=(rows||[]).filter(x=>!lastSeen||String(x.created_at)>lastSeen).length;const badge=opener?.querySelector('.uon-notification-badge');if(badge){badge.hidden=!unread;badge.textContent=unread>99?'99+':String(unread)}items.innerHTML=(rows||[]).length?(rows||[]).map(x=>{const url=safeNotificationUrl(x.url);const body=`<article class="notification-item"><span>${esc(x.icon||'🔔')}</span><div><strong>${esc(x.title||'إشعار')}</strong>${x.body?`<p>${esc(x.body)}</p>`:''}<small>${new Date(x.created_at).toLocaleDateString('ar-OM')}</small></div></article>`;return url?`<a href="${esc(url)}" class="notification-link" ${url.startsWith('http')?'target="_blank" rel="noopener"':''}>${body}</a>`:body}).join(''):'<div class="empty">لا توجد إشعارات حاليًا.</div>';opener?.addEventListener('click',()=>{const newest=rows?.[0]?.created_at;if(newest)localStorage.setItem('uon_notifications_seen_at',newest);if(badge)badge.hidden=true},{once:true})}catch(error){items.innerHTML='<div class="empty">تعذر تحميل الإشعارات حاليًا.</div>';reportClientError(error,'notifications')}}
let lastReported='';async function reportClientError(error,source='browser'){try{const message=String(error?.message||error||'Unknown browser error').slice(0,500);const signature=`${source}:${message}`;if(signature===lastReported)return;lastReported=signature;await rpc('uon_report_client_error',{p_message:message,p_source:source,p_details:{page:location.pathname,user_agent:navigator.userAgent.slice(0,300),stack:String(error?.stack||'').slice(0,1500)}})}catch{}}
window.addEventListener('error',event=>reportClientError(event.error||event.message,'window.error'));
window.addEventListener('unhandledrejection',event=>reportClientError(event.reason,'unhandledrejection'));
const isAdminPage=document.body.classList.contains('admin-page')||/\/admin(?:\.html)?\/?$/.test(location.pathname);
if(!isAdminPage){applyManagedFooter();setupNotifications()}
window.UON_V20={favorites,toggleFavorite,contributions:()=>readJson(contributionKey,[]),reportClientError};
