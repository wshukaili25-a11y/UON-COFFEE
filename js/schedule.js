
import {$,setupNav} from './ui.js';setupNav();const days=['الأحد','الاثنين','الثلاثاء','الأربعاء','الخميس'];const times=['8:00','9:30','11:00','12:30','2:00','3:30'];const grid=$('#scheduleGrid');grid.innerHTML='<div class="schedule-cell"></div>'+days.map(d=>`<div class="schedule-cell"><b>${d}</b></div>`).join('');times.forEach((t,ri)=>{grid.insertAdjacentHTML('beforeend',`<div class="schedule-cell"><b>${t}</b></div>`);days.forEach((d,ci)=>grid.insertAdjacentHTML('beforeend',`<div class="schedule-cell"><input data-k="${ri}-${ci}" value="${localStorage.getItem('sched-'+ri+'-'+ci)||''}" placeholder="المادة / القاعة"></div>`))});grid.querySelectorAll('input').forEach(i=>i.oninput=()=>localStorage.setItem('sched-'+i.dataset.k,i.value));$('#clearSchedule').onclick=()=>{grid.querySelectorAll('input').forEach(i=>{i.value='';localStorage.removeItem('sched-'+i.dataset.k)})};$('#printSchedule').onclick=()=>print();
