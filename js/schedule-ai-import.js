const PENDING='uon_ai_pending_schedule_v1',TARGET='uon-v7-schedule';
function uid(){try{return crypto.randomUUID()}catch{return`${Date.now()}-${Math.random()}`}}
const dayMap={Sunday:'الأحد',Monday:'الاثنين',Tuesday:'الثلاثاء',Wednesday:'الأربعاء',Thursday:'الخميس','الأحد':'الأحد','الاثنين':'الاثنين','الثلاثاء':'الثلاثاء','الأربعاء':'الأربعاء','الخميس':'الخميس'};
function apply(){let p;try{p=JSON.parse(sessionStorage.getItem(PENDING)||'null')}catch{return}if(!p?.rows?.length)return;const grouped=new Map(),out=[];for(const r of p.rows){const day=dayMap[String(r.day||'').trim()];const start=String(r.start||'').slice(0,5),end=String(r.end||'').slice(0,5);if(!day||!/^([01]\d|2[0-3]):[0-5]\d$/.test(start)||!/^([01]\d|2[0-3]):[0-5]\d$/.test(end)||end<=start)continue;const key=[r.course,start,end,r.room||'',r.teacher||''].join('|');if(!grouped.has(key))grouped.set(key,uid());out.push({id:uid(),seriesId:grouped.get(key),course:String(r.course||'').trim().toUpperCase(),day,start,end,room:String(r.room||''),teacher:String(r.teacher||''),type:'lecture'})}if(!out.length)return;try{const current=JSON.parse(localStorage.getItem(TARGET)||'[]');if(current.length){const ok=confirm(document.documentElement.lang?.startsWith('en')?'Replace your current schedule with the UON AI suggestion?':'استبدال جدولك الحالي باقتراح UON AI؟');if(!ok)return}localStorage.setItem(TARGET,JSON.stringify(out));sessionStorage.removeItem(PENDING);sessionStorage.setItem('uon_ai_schedule_applied_v1','1')}catch(error){console.warn('AI schedule import failed',error)}}
apply();