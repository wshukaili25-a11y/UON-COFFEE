// UON AI restored conversations intentionally stay text-first.
// Rich course, calendar, contact and visual cards were removed so reopening
// a conversation preserves the same clean conversational experience.
document.addEventListener('uon:assistant-history-restored',()=>{});
