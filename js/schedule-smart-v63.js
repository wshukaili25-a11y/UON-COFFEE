const STORAGE_KEY='uon-v7-schedule';
const DAY_NAMES={Sun:'الأحد',Mon:'الاثنين',Tue:'الثلاثاء',Wed:'الأربعاء',Thu:'الخميس',Fri:'الجمعة',Sat:'السبت'};
const esc=value=>String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
let timer=null;
function readRows(){try{const rows=JSON.parse(localStorage.getItem(STORAGE_KEY)||'[]');return Array.isArray(rows)?rows:[]}catch{return[]}}
function mins(value){const m=/^(\d{1,2}):(\d{2})$/.exec(String(value||''));return m?Number(m[1])*60+Number(m[2]):0}
function clock(total){const h=Math.floor(total/60),m=String(total%60).padStart(2,'0');return`${h%12||12}:${m} ${h<12?'ص':'م'}`}
function muscatNow(){const parts=new Intl.DateTimeFormat('en-US',{timeZone:'Asia/Muscat',weekday:'short',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(new Date());const map=Object.fromEntries(parts.map(p=>[p.type,p.value]));return{day:DAY_NAMES[map.weekday],minutes:Number(map.hour)*60+Number(map.minute)}}
function todayRows(){const now=muscatNow();return readRows().filter(row=>row.day===now.day).map(row=>({...row,startMin:mins(row.start),endMin:mins(row.end)})).sort((a,b)=>a.startMin-b.startMin)}
function state(row,now){if(now>=row.endMin)return'done';if(now>=row.startMin)return'live';return'next'}
function inject(){if(document.querySelector('#scheduleSmartV63'))return;const shell=document.querySelector('.schedule-shell');if(!shell)return;const section=document.createElement('section');section.id='scheduleSmartV63';section.className='card schedule-smart-v63';section.innerHTML=`<div class="schedule-smart-head"><div><span class="student-label">يومك الآن</span><h2>عرض اليوم</h2><p>شوف محاضرات اليوم بسرعة وابدأ Focus للمادة مباشرة.</p></div><div class="schedule-smart-actions"><a class="btn" href="assistant.html?prompt=${encodeURIComponent('وش عندي اليوم؟')}">اسأل UON AI</a><a class="btn" href="study-plan.html">خطة مذاكرة</a></div></div><div id="scheduleTodayTimeline" class="schedule-today-timeline"></div>`;const board=document.querySelector('.schedule-board-card');if(board)board.before(section);else shell.prepend(section);render();const week=document.querySelector('#week');if(week)new MutationObserver(()=>{clearTimeout(timer);timer=setTimeout(render,120)}).observe(week,{childList:true,subtree:true});window.addEventListener('storage',e=>{if(e.key===STORAGE_KEY)render()});setInterval(render,60000)}
function render(){const host=document.querySelector('#scheduleTodayTimeline');if(!host)return;const now=muscatNow(),rows=todayRows();if(!rows.length){host.innerHTML=`<div class="schedule-today-empty"><strong>${esc(now.day||'اليوم')} فاضي 🎉</strong><span>ما عندك محاضرات محفوظة اليوم.</span><a class="btn" href="study-focus.html?minutes=25">ابدأ Focus عام</a></div>`;return}host.innerHTML=rows.map(row=>{const s=state(row,now.minutes),label=s==='live'?'الآن':s==='done'?'انتهت':'قادمة';const countdown=s==='next'?` · بعد ${Math.max(0,row.startMin-now.minutes)} د`:s==='live'?` · باقي ${Math.max(0,row.endMin-now.minutes)} د`:'';return`<article class="schedule-today-row ${s}"><div class="schedule-today-state"><span></span><small>${label}${countdown}</small></div><div class="schedule-today-course"><strong>${esc(row.course)}</strong><span>${clock(row.startMin)} – ${clock(row.endMin)}${row.room?` · ${esc(row.room)}`:''}${row.teacher?` · ${esc(row.teacher)}`:''}</span></div><div class="schedule-today-actions"><a class="btn" href="study-focus.html?course=${encodeURIComponent(row.course||'')}&minutes=25">Focus</a><a class="btn" href="course.html?code=${encodeURIComponent(row.course||'')}">المادة</a></div></article>`}).join('')}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',inject,{once:true});else inject();