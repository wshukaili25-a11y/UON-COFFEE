const read=(k,f)=>{try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(f))}catch{return f}};const fav=read('uon_favorites_v20',[]),contrib=read('uon_contributions_v20',[]),pref=read('uon_notification_preferences',{});document.querySelector('#favCount').textContent=fav.length;document.querySelector('#contribCount').textContent=contrib.length;document.querySelector('#prefCount').textContent=(pref.topics||[]).length;document.querySelector('#favoriteRows').innerHTML=fav.length?fav.map(x=>`<a href="${x.url}"><strong>${x.title}</strong><span>فتح ←</span></a>`).join(''):'<div class="empty">لا توجد صفحات محفوظة بعد. استخدم زر ♡ في أي صفحة.</div>';