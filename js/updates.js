import{get,esc}from'./core.js';try{const r=await get('site_updates','select=*&active=eq.true&order=created_at.desc&limit=100');document.querySelector('#updatesRows').innerHTML=r.length?r.map(x=>`<article class="v20-update-card"><span>${esc(x.icon||'✨')}</span><h3>${esc(x.title)}</h3><p>${esc(x.body||'')}</p><small>${new Date(x.created_at).toLocaleDateString('ar')}</small></article>`).join(''):'<div class="empty">لا توجد تحديثات منشورة.</div>';localStorage.setItem('uon_updates_seen',String(r.length))}catch(e){document.querySelector('#updatesRows').innerHTML='<div class="empty">تعذر تحميل التحديثات.</div>'}