import {setupNav,enforceUonMaintenance,watchUonMaintenance,$,get,notifyPending,toast,esc,openModal,closeModal,rpc} from './core.js';
setupNav();await enforceUonMaintenance();watchUonMaintenance();
const SESSION_KEY='uon_project_submission_session_v2';let sessionId=localStorage.getItem(SESSION_KEY);if(!sessionId){sessionId=crypto.randomUUID();localStorage.setItem(SESSION_KEY,sessionId)}
let rows=[];
async function load(){try{rows=await get('public_student_projects','select=id,title,owner_name,major,study_year,description,github_url,demo_url,image_url,status,featured,created_at,views,likes,updated_at,url&order=created_at.desc');render()}catch(e){toast(e.message,true)}}
function render(){const q=$('#search').value.toLowerCase();const list=rows.filter(x=>`${x.title||''} ${x.major||''} ${x.description||''}`.toLowerCase().includes(q));$('#items').innerHTML=list.length?list.map(x=>`<article class="card item-card"><span class="badge">${esc(x.major||'مشروع طلابي')}</span><h3>${esc(x.title)}</h3><p>${esc(x.description||'')}</p><small>${esc(x.owner_name||'')}</small><div class="actions" style="margin-top:12px"><a class="btn primary" target="_blank" rel="noopener noreferrer" href="${esc(x.url||x.demo_url||x.github_url||'#')}">فتح المشروع</a></div></article>`).join(''):'<div class="empty">لا توجد مشاريع معتمدة حاليًا</div>'}
$('#search').oninput=render;$('#openForm').onclick=()=>openModal('modal');$('#close').onclick=()=>closeModal('modal');
$('#form').onsubmit=async e=>{e.preventDefault();const form=e.currentTarget;if(form.dataset.submitting==='1')return;const body=Object.fromEntries(new FormData(form));const button=form.querySelector('[type="submit"]');form.dataset.submitting='1';button.disabled=true;const original=button.textContent;button.textContent='جاري الإرسال...';try{const id=await rpc('uon_submit_student_project_v2',{p_title:String(body.title||'').trim(),p_owner_name:String(body.owner_name||'').trim(),p_major:String(body.major||'').trim()||null,p_study_year:String(body.study_year||'').trim()||null,p_description:String(body.description||'').trim(),p_project_url:String(body.url||'').trim(),p_image_url:String(body.image_url||'').trim()||null,p_contact:String(body.contact||'').trim(),p_session_id:sessionId});await notifyPending('student_projects',id);toast('تم إرسال المشروع للمراجعة');closeModal('modal');form.reset()}catch(err){toast(err.message||'تعذر إرسال المشروع',true)}finally{form.dataset.submitting='0';button.disabled=false;button.textContent=original}};
load();